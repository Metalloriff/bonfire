extends VBoxContainer


# func _on_tab_container_tab_changed(tab: int) -> void:
# 	var tabs: TabContainer = $TabContainer

# 	for i in tabs.get_tab_count():
# 		tabs.set_tab_title(i, tabs.get_child(i).name if i == tab else "")
